apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ferriskey-tls
  namespace: ferriskey
spec:
  secretName: ferriskey-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - sso.gsaab.dev
